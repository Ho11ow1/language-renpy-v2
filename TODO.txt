1. Update data/renpy.json to show everything available, depth: TBD probably just the basic documented API but we'll see if i want to just use the docs or create an actual script for extraction
2. Implement some basic diagnostics: Tabs vs Space | Misspelled default or define | reserved renpy stuff | bad var names | bad indent level | etc...
3. Implement definition.ts to use the same thing hover does so turn that into the contextparser at some point.
4. Maybe also add the color picker the OG had, we'll see if people want that though
5. Dreaded syntax highlighting