1. Re-structure JSON
2. Somehow structure things in a way where in screen language we get screen stuff but outside of screen lang we get what we get now
3. Figure out why screen help is showing up as a function (Parser) and why it's being detected as internal when it also shows the definition file (Yep, parser again)
4. Figure out why default quick_menu isn't detected as a var but the screen quick_menu() does get detected as a screen (Parser once again)
5. Do a complete test run once everything is working and get's detected properly, maybe also get another person to test but we'll see how that goes
6. After all that either start working on diagnostics or syntax highlighting. Probably diagnostics though