import * as fs from "fs";
import { Store } from "@src/store";

export class WorkspaceParser
{
    public static ParseFile(path: string): void
    {
        // Parse then call over to Store to update the specified instance
    }

    
}